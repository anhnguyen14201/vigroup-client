import React from 'react'
import { TemplateClient } from '@/components'

export const metadata = {
  title: 'Mẫu thiết kế',
  description: 'Trang quản lý danh sách dự án, gồm tìm kiếm, thêm, sửa, xóa',
}

const TemplatePage = () => {
  return <TemplateClient />
}

export default TemplatePage
