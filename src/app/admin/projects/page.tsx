import { ProjectClient } from '@/components'

export const metadata = {
  title: 'Quản lý dự án',
  description: 'Trang quản lý danh sách dự án, gồm tìm kiếm, thêm, sửa, xóa',
}
const ProjectPage = () => {
  return <ProjectClient />
}

export default ProjectPage
